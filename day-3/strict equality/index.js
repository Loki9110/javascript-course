// = assignment operator
// == comparison operator (compare if values are equal )
// === strict equality operator (compares if values and operators are same)
// != not equal operator
// !== strict inequality operator

const pi = 3.14;

if (pi === "3.14"){
    console.log("this is pi");
}
else {
    console.log("this is not pi");
}
