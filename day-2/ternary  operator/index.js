// ternary operator = a shortcut to if() an else() statements helps to assign a variable  based on a condition ? codeiftrue : codeiffalse;


let age = 18;

let msg = age>=18 ? "you can vote" : "you cant vote";

console.log(`msg`);

