// array = a variable like structure that can hold more than 1 value

let games = ["fortnite","days gone","outer worlds","resident evil"];
games[1] = "starfield";
games.push("forza horizon 5");   // add element at the end
games.pop();                     // remove element from the end
games.unshift("warzone");        // add element from the begeninng
games.shift();                   // remove element from the begenning

console.log(games);
console.log(games[2]);
console.log(games.length);   // length of an array
console.log(games.indexOf("starfield"));

for (i=0;o<fruits.length;i++){
    console.log(fruits[i]);
}
